# GPS Issue & Management Center — 開發建置規格書 v0.4

This file is preserved verbatim from the originating specification document.
The implementation code reads it for context; treat it as the source of
truth for requirements. ADRs override the spec only where they explicitly
say they do.

**Version** REV 2026.05.D
**Status** working draft — integrated ChatGPT architecture feedback +
internal four-aspect self-audit
**Author** Rockie Lin
**Supersedes** v0.3 (REV 2026.05.C)

See `/docs/adr/` for the decisions distilled from this spec.
See `/docs/architecture.md` for the implementation map.
See `README.md` for the 30-minute local start.

(Full prose spec lives in the project root and is intentionally not
duplicated here to keep this file from drifting from the source. The
implementation references the spec by section number throughout — see
the section-tag comments at the top of each `lib/` file.)
